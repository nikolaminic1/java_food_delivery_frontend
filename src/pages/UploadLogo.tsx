import React, { FC, ReactElement } from "react";

interface UploadLogoProps {}

const UploadLogo: FC<UploadLogoProps> = ({}): ReactElement => {
  return <div>Upload logo</div>;
};

export default UploadLogo;
